<%= form_for(topic) do |f| %>
  <% if topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
      <div class="col s12 m3"></div>
      <div class="col s12 m6">
        <div class="card blue-grey darken-1">
          <div class="card-content white-text">
            <div class="row">
                <div class="col s12 m2"><h5>Title</h5></div>
                <div class="col s12 m10"><%= f.text_field :title %></div>
            </div>
            <div class="row">
                <div class="col s12 m3"><h5>Description</h5></div>
                <div class="col s12 m9"><%= f.text_field :description %></div>
            </div>
          </div>
          <div class="card-action">
              <div class="col s12 m8"></div>
              <button class="btn btn-large waves-effect waves-light">
                  <%= f.submit %>
              </button>
          </div>
        </div>
      </div>
      <div class="col s12 m3"></div>
  </div>
<% end %>
